# Pi skills

Put your global Pi skills here.

Each skill should live in its own directory with a `SKILL.md`, for example:

```text
skills/
  my-skill/
    SKILL.md
```

This directory is symlinked into `~/.pi/agent/skills` by `./install.sh pi`.
