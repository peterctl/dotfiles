" Do not wrap long lines.
set nowrap

" Automatically indent new lines.
set autoindent

" Insert spaces on tab press.
set expandtab

" Smarter tab and backspace handling.
set smarttab

" Use 4 space indentation.
set shiftwidth=4

" Set tab stops every 4 spaces.
set tabstop=4

" Let backspace delete indents.
set softtabstop=4

" Gutentag tags output folder.
let g:gutentags_cache_dir = '~/.cache/nvim/gutentags'

" Enable the autocompletion system at startup.
let g:deoplete#enable_at_startup = 1
